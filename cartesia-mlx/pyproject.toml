[build-system]
requires = [
  "setuptools>=42",
  "cmake>=4.1.2",
  "mlx>=0.29.4",
  "nanobind==2.4.0",
  "transformers>=4.40.0",
  "datasets",
  "torch",
  "numpy",
  "requests",
  "huggingface_hub"
]
build-backend = "setuptools.build_meta"
